{
  "name": "@theworld/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "main": "dist/main.js",
  "scripts": {
    "check": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json",
    "dev": "pnpm exec tsc -p tsconfig.json && env -u ELECTRON_RUN_AS_NODE electron ."
  },
  "devDependencies": {
    "electron": "^37.2.0"
  },
  "dependencies": {
    "@theworld/shared-contracts": "workspace:*",
    "katex": "^0.16.45"
  }
}
