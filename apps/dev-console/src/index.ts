/**
 * Default package entry: interactive Chinese first-layer demo (requires OPENAI_* env).
 * One-shot / smoke: `pnpm demo:live`；mock: `pnpm demo:mock`（见 package.json）。
 */
import './demo-interactive.js'
